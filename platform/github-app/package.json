{
  "name": "@ghfind/github-app",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "wrangler types --strict-vars=false",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "wrangler deploy --env= --dry-run --outdir dist"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.1.4",
    "@types/node": "24.10.1",
    "typescript": "5.9.3",
    "vitest": "4.1.0",
    "wrangler": "4.131.2"
  }
}
